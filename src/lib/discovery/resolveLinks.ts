// DEPRECATED and unused. The old "re-search and swap the link" approach caused
// wrong-product links (image of product A, link to product B). It has been
// replaced by the Exa adapter (exa.ts), which searches retailers directly and
// returns correctly-matched first-party product pages. Kept as an empty module
// to avoid breaking any stale import; safe to delete.
export {};
